fastapi==0.111.0
uvicorn==0.22.0
sqlalchemy==2.0.41  # Your specified version
psycopg2-binary==2.9.9
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
python-dotenv==1.0.0
pydantic==2.5.0
requests==2.32.3
tenacity==8.2.3
pytz==2024.1
python-dateutil==2.8.2
# Upgrade conflicting packages to latest compatible
certifi==2025.7.14  # Latest, satisfies selenium>=2025.4.26
urllib3==2.4.0  # Matches selenium's ~=2.4.0
# If selenium is needed (unlikely for FastAPI backend, but if so):
# selenium==4.35.0  # Latest version